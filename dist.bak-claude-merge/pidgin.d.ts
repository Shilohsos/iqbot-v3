/** Replace @username placeholder with the actual Telegram first name. */
export declare function resolveUsername(template: string, firstName: string): string;
export declare function applyPidgin(text: string): string;
